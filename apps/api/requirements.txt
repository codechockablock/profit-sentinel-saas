# Core dependencies - pinned for reproducibility
fastapi==0.115.0
# starlette version is managed by fastapi - don't pin separately
uvicorn[standard]==0.32.0
python-multipart==0.0.18
pandas==2.2.3
openpyxl==3.1.5
boto3==1.35.0
python-dotenv==1.0.1
supabase==2.10.0
openai==1.55.0
pydantic[email]==2.9.2
pydantic-settings==2.6.0
httpx==0.27.2

# Email service dependencies
resend==2.4.0
sendgrid==6.11.0

# Payment processing
stripe==11.3.0

# ML dependencies
torch>=2.2.0
torchvision>=0.17.0

# Dorian/Diagnostic dependencies
numpy>=1.24.0
faiss-cpu>=1.7.4
reportlab>=4.0.0
SPARQLWrapper>=2.0.0

# Testing dependencies
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Rate limiting
slowapi==0.1.9

# Security note: Run `pip-audit` periodically to check for CVEs
